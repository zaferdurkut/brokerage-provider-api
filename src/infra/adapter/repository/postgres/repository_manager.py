from src.infra.adapter.repository.postgres.repository_config import SessionLocal


class RepositoryManager:
    def __init__(self):
        self.db = SessionLocal()

    def __enter__(self):
        return self.db

    def __exit__(self, exc_type, exc_value, traceback):
        self.db.close()
